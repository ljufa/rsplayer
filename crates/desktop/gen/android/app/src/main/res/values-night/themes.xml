<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. The window background shows behind the
         transparent status/navigation bars, so it matches the web UI's
         default dark base colour. -->
    <style name="Theme.rsplayer_desktop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/rsplayer_window_background</item>
    </style>
</resources>
