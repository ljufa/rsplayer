name: Build and test
on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - '**.rs'
      - '**.toml'
      - '**/ci.yml'
env:
  CARGO_TERM_COLOR: always

jobs:
    
  build_backend:
    name: Build and test
    runs-on: rsplayer-self-hosted
    steps:
    - uses: actions/checkout@v5
    - name: Clippy lint
      run: cargo make clippy
    - name: Run tests
      run: cargo make test
